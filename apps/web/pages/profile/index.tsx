import React from 'react';
import './profile.scss';

export interface ProfileProps {}

export const Profile = (props: ProfileProps) => {
  return (
    <div>
      You found the Profile page! 
    </div>
  );
};

export default Profile;
